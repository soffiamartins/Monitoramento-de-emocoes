import express from 'express';

const router = express.Router();

// Em apiRoutes.js
router.get('/moods/month', (req, res) => {
  // Buscar humores do mês atual do usuário
  // Retornar JSON com formato correto
});
