<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>

    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">

    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-bg-color: #344284;
            --card-bg-color: #d8d8d8;
            --button-bg-color: #6b83d2;
            --button-hover-color: #2e306b;
            --input-border-color: #ccc;
            --text-light: white;
            --card-text-color: #333;
        }

        body {
            font-family: "Roboto", sans-serif;
            background-color: var(--primary-bg-color);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 40px;
            color: var(--text-light);
        }

        .layout {
            width: 100%;
            max-width: 1200px;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .left {
            font-size: 3.6rem;
            font-weight: 700;
            padding-left: 40px;
        }

        .right .container {
            background-color: var(--card-bg-color);
            padding: 40px;
            border-radius: 12px;
            width: 420px;
            color: var(--card-text-color);
        }

        .title {
            font-size: 1.9rem;
            font-weight: 700;
            margin-bottom: 4px;
        }

        .subtitle {
            font-size: 0.95rem;
            color: #666;
            margin-bottom: 30px;
        }

        .top-buttons {
            display: flex;
            justify-content: flex-end;
            gap: 10px;
            margin-bottom: 25px;
        }

        .top-buttons button {
            width: 120px; 
            padding: 10px 0;
            border-radius: 6px;
            border: none;
            background-color: #fff8f8;
            cursor: pointer;
        }


        .top-buttons .main {
            background-color: var(--button-bg-color);
            color: white;
        }

        .top-buttons .main:hover {
            background-color: var(--button-hover-color);
        }

        input {
            width: 100%;
            padding: 12px;
            border-radius: 6px;
            border: 1px solid var(--input-border-color);
            margin-bottom: 15px;
        }

        button.submit {
            width: 100%;
            padding: 12px;
            border-radius: 6px;
            border: none;
            background-color: var(--button-bg-color);
            color: var(--text-light);
            font-size: 1rem;
            font-weight: 700;
            cursor: pointer;
            margin-top: 5px;
        }

        button.submit:hover {
            background-color: var(--button-hover-color);
        }

        .links {
            margin-top: 15px;
            text-align: center;
        }

        .links a {
            display: block;
            color: black;
            font-size: 0.9rem;
            text-decoration: none;
            margin-top: 6px;
        }

        .links a:hover {
            text-decoration: underline;
        }

 

        .password-wrapper {
            position: relative;
            width: 100%;
        }

        .eye-icon {
            width: 22px;
            height: 22px;
            position: absolute;
            right: 12px;
            top: 50%;
            transform: translateY(-50%);
            cursor: pointer;

            stroke: #ffffff;
            fill: #555555;
        }

        .hidden {
            display: none;
        }

        @media(max-width: 900px) {
            .layout {
                flex-direction: column;
                text-align: center;
            }

            .left {
                margin-bottom: 35px;
                padding: 0;
            }
        }
        .divider {
            width: 100%;
            height: 1px;
            background-color: #b5b5b5;
            margin: 20px 0;
            opacity: 0.8;
        }

    </style>
</head>

<body>

    <div class="layout">

        <div class="left">MoodBit.</div>

        <div class="right">
            <div class="container">

                <div class="title">Comece sua jornada.</div>
                <div class="subtitle">Acompanhe suas emoções diariamente</div>

                <div class="top-buttons">
                    <button class="main">Entrar</button>
                    <button onclick="window.location.href='/register'">Criar conta</button>
                </div>
                <div class="divider"></div>


                <% if (error) { %>
                <div class="error"><%= error %></div>
                <% } %>

                <form method="POST" action="/login">

                    <input type="email" name="email" placeholder="Email" required>

              
                    <div class="password-wrapper">
                        <input type="password" name="password" id="password" placeholder="Senha" required>

                        <svg id="icon-eye" class="eye-icon" viewBox="0 0 24 24">
                            <path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7S1 12 1 12z" />
                            <circle cx="12" cy="12" r="3" />
                        </svg>

                        <svg id="icon-eye-off" class="eye-icon hidden" viewBox="0 0 24 24">
                            <path d="M17.94 17.94A10.94 10.94 0 0 1 12 20c-7 0-11-8-11-8a21.77 21.77 0 0 1 5.06-6.94" />
                            <path d="M1 1l22 22" />
                        </svg>
                    </div>

                    <button type="submit" class="submit">Entrar na minha conta</button>
                </form>

                <div class="links">
                    <a href="#">Esqueceu a senha?</a>
                </div>

            </div>
        </div>

    </div>


    <script>
        const passwordInput = document.getElementById("password");
        const eye = document.getElementById("icon-eye");
        const eyeOff = document.getElementById("icon-eye-off");

        eye.addEventListener("click", () => {
            passwordInput.type = "text";
            eye.classList.add("hidden");
            eyeOff.classList.remove("hidden");
        });

        eyeOff.addEventListener("click", () => {
            passwordInput.type = "password";
            eyeOff.classList.add("hidden");
            eye.classList.remove("hidden");
        });
    </script>

</body>

</html>